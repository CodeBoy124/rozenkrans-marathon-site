---
import { Image } from "astro:assets";

interface Props {
    src: ImageMetadata;
    alt: string;
    classes?: string[];
}

const {src, alt, classes = []} = Astro.props;

---
<Image src={src} alt={alt} class:list={["rounded-2xl", "shadow-md/25", ...classes]}/>
