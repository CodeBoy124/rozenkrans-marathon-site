---

interface Props {
    href: string;
    classes?: string[]
}

const {href, classes = []} = Astro.props;

---

<a href={href}>
    <div class:list={["w-12", "h-12", "rounded-full", "flex", "justify-center", "items-center", "inset-shadow-sm/25", ...classes]}>
        <slot />
    </div>
</a>